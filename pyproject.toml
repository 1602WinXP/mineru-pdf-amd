[project]
name = "mineru-local"
version = "1.0.0"
description = "MinerU local deployment toolkit — API client, markdown cleaner, and comprehensive deployment guides"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.28",
    "httpx>=0.27",
]

[project.scripts]
mineru-api-client = "mineru_api_client:main"
mineru-md-clean = "mineru_md_clean:main"
